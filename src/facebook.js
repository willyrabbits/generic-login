/**
 * https://developers.facebook.com/apps
 * 
 * creamos app
 * copiamos id + password a firebase->habilitar facebook
 * añadimos plataforma con nombre com.expoapp.mobile
 */

export default {
    config: {
        application_id: '370895880238369',
        permissions: ['public_profile']
    }
};